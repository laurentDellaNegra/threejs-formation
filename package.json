{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "create-dist": "chmod +x create-dist.sh && ./create-dist.sh",
    "build-and-create-dist": "turbo run build && chmod +x create-dist.sh && ./create-dist.sh",
    "dev": "turbo run dev --no-cache --continue",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md,html,css}\""
  },
  "devDependencies": {
    "prettier": "^2.8.0",
    "turbo": "^1.10.14"
  }
}